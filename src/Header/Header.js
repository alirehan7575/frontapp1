import React from 'react'
import styles from './Header.module.css'

export const Header = () => {

    return <div className={`bg-primary text-center text-white ${styles.header} fs`}>
        My First React Application
    </div>
}

